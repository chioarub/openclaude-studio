// Shared UI primitives re-exported from App.tsx so feature pages extracted into
// separate files can reuse the studio's design language (PageStack, PageHeader,
// QuickStat, SectionHeading, Badge, EmptyState) without duplicating markup.
export {
  Badge,
  EmptyState,
  PageHeader,
  PageStack,
  QuickStat,
  SectionHeading,
} from '../App.js';
