# Documentation Assets

Screenshots in this directory must use synthetic OpenClaude data only.

Do not commit images that include real prompts, provider credentials, debug logs, session contents, personal data, or machine-specific paths. Review every screenshot manually before linking it from public docs.
